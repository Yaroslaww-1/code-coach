export enum AppRoute {
  BASE = "/",

  COMMUNITIES = "/communities",
  COMMUNITY = "/communities/:id",

  POSTS = "/posts",
  POST = "/posts/:id",
  
  USERS = "/users",
  USER = "/users/:id",
  LOGIN = "/login",

  STUDENTS = "/students",

  CHAT = "/chats/:id",
}