export enum AppRoute {
  COMMUNITIES = "/communities",
  COMMUNITY = "/communities/:id",

  POSTS = "/posts",
  POST = "/posts/:id",
  
  USERS = "/users",
  USER = "/users/:id",
  LOGIN = "/login",

  COACH = "/coach",

  CHAT = "/chats/:id",
}