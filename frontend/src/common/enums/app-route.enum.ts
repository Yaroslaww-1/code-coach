export enum AppRoute {
  BASE = "/",

  COMMUNITIES = "/communities",
  COMMUNITY = "/communities/:id",

  POSTS = "/posts",
  POST = "/posts/:id",
  
  USERS = "/users",
  USER_MY = "/users/my",
  USER = "/users/:id",
  LOGIN = "/login",

  STUDENTS = "/students",

  CHAT = "/chats/:id",

  COACHES = "/coaches",
  COACHES_MY = "/coaches/my",
}