describe("Community", () => {
  describe("createNew", () => {
    it("returns new community", async () => {
      return true;
    });
  });

  describe("editName", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws exception when input empty string", async () => {
      return true;
    });
  });

  describe("createPost", () => {
    it("returns new post", async () => {
      return true;
    });
  });

  describe("leave", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws exception when user not a member", async () => {
      return true;
    });
  });

  describe("join", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws exception when user already a member", async () => {
      return true;
    });
  });

  describe("findMentor", () => {
    it("returns a mentor", async () => {
      return true;
    });
  });
});