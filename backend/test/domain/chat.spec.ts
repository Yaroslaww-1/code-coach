describe("Chat", () => {
  describe("createNew", () => {
    it("returns new community", async () => {
      return true;
    });
  });

  describe("sendMessage", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws exception when input empty string", async () => {
      return true;
    });

    it("throws exception when user not in chat", async () => {
      return true;
    });
  });
});