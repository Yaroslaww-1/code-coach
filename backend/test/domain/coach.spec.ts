describe("Coach", () => {
  describe("setProgrammingLanguages", () => {
    it("returns void when multiple languages", async () => {
      return true;
    });

    it("returns void when no languages", async () => {
      return true;
    });
  });

  describe("setLanguages", () => {
    it("returns void when multiple languages", async () => {
      return true;
    });

    it("returns void when no languages", async () => {
      return true;
    });
  });

  describe("yearsOfExperience", () => {
    it("returns correct value when one company", async () => {
      return true;
    });

    it("returns correct value when two companies", async () => {
      return true;
    });

    it("returns correct value when no end date", async () => {
      return true;
    });
  });

  describe("requestMentorship", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws an exception if user is not student", async () => {
      return true;
    });

    it("throws an exception if user is already a mentee", async () => {
      return true;
    });
  });

  describe("applyMentorship", () => {
    it("returns void", async () => {
      return true;
    });

    it("throws an exception if user is not student", async () => {
      return true;
    });

    it("throws an exception if there was no request", async () => {
      return true;
    });
  });
});