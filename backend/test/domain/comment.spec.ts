describe("Comment", () => {
  describe("replyNew", () => {
    it("returns new comment", async () => {
      return true;
    });
  });

  describe("reply", () => {
    it("returns new comment", async () => {
      return true;
    });
  });
});